export class FilmModel {
  constructor(public id: string, public title: string, public description: string, public imageUrl
    ,         public direction: string, public type: string, public production: string, public year: number) {
  }
}
